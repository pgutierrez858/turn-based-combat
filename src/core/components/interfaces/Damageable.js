export const Damageable = {
  getCurrentHp: () => {},
  getMaxHp: () => {},
  /**
   * Apply given amount of damage to this entity.
   * @param {integer} amount
   */
  takeDamage: (amount) => {},
}; // Damageable
